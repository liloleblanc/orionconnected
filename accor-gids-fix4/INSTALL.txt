GIDS FIXES — round 4 (Accor hierarchy)
======================================
Replace in repo (fids-current/), then: npx wrangler deploy

  js/fids-core.js     -> fids-current/js/fids-core.js
  css/accor-slide.css -> fids-current/css/accor-slide.css

THIS ROUND — Accor slide hierarchy now correct:
- Hotel LOGO (Novotel) was rendering TINY because the SVG floats its wordmark
  in a big square viewBox (used only ~25% of height). Now cropped to ink bounds
  via the runtime helper -> logo fills its box and is the BIGGEST element.
  LOGO 100% > NAME 75% > INFO ~62%. (Novotel crop added; more brands can be
  added to _LOGO_CROP map as needed.)
- "MEMBRE DE" dialed back DOWN (was too big at 18-26px -> now 12-17px medium).
- Hotel logos forced white + filled to box by the crop helper.

INCLUDED FROM EARLIER ROUNDS:
- AC globe ad overflow fix; Accor slide shrunk ~25%; endorsement grouped together.

NOTE: AC airline IMAGE ads (roundel "IR CANAD", route map) still overflow —
those are a separate pending fix (cover-vs-contain on baked-text marketing
assets). Not addressed in this round.

Deploy: npx wrangler deploy -> "Uploaded fids" -> hard-refresh.
