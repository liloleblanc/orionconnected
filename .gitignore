# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# macOS Finder metadata
.DS_Store
**/.DS_Store

# tooling installed ad hoc during sessions — never part of the site
node_modules/
package.json
package-lock.json
probe.js
# local test symlink to leaflet (prod serves /mapcdn from the worker)
fids-current/mapcdn
