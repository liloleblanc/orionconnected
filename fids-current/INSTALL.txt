GIDS — FIT + TIMING + ORDER + WIDER AD BOX
==========================================
Replace in repo (fids-current/), then: npx wrangler deploy
(includes ALL prior fixes: Accor slide, endorsement, logo crop, globe, etc.)

FILES
  js/fids-core.js        renderer: per-item fit/zoom/position + per-item dwell + media sorted by order
  js/local-media.js      stores fit/zoom/posX/posY/dwellMs/order; update() method
  js/menu.js             "Adjust" button on each media item
  js/media-adjust.js     preview/adjust panel (fit, zoom, position, DURATION, ORDER)
  css/gids-v218.78.css   columns now 20% / 60% / 20% (shrunk both sides, wider ad box)
  css/accor-slide.css    (Accor slide styles, unchanged this round)
  gids/fids/bids.html    load media-adjust.js + cache bumps

WHAT'S NEW THIS ROUND
- FIT: uploaded ads default to "contain" (no crop); adjustable per item.
- TIMING: each media item has its own Duration (seconds) — set in Adjust panel.
- ORDER: each item has a Play order number; media plays 1,2,3,4… and the
  3:1 Accor interleave still applies on top.
- WIDER AD BOX: left + right gate columns shrunk equally (25%->20%); center
  ad column widened (50%->60%) so ads have more room and a better shape.

USE: Console -> Media Library -> Adjust on any item -> set fit/zoom/position/
duration/order -> Save.

NOTE: built-in AIRLINE ads (AC globe, AA, route maps) still a separate pending
item — not changed here.

Deploy: npx wrangler deploy -> "Uploaded fids" -> hard-refresh.
