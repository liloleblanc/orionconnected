ADS ONLY ACCOR V19 - TEXT SAFE AREA / BRAND LOCK

What changed:
1. Added a final, last-in-file CSS lock for the Accor hotel card so the ad panel cannot draw under the aircraft/map column.
2. Reduced usable panel padding to create an actual safe area instead of wasting the narrow text column.
3. Locked the centre ad grid to 58% photo / 42% hotel panel, matching the live screenshots.
4. Forced Novotel wordmark, Fairmont wordmark, property name, address, copy, rating, amenities, and ALL footer to fit inside the panel.
5. Added line clamps and overflow rules so long hotel names/descriptions cannot stack through the footer again.
6. Added a renderer-side copy fuse in js/fids-core.js. Full hotel paragraphs are shortened before layout, so CSS is not carrying the whole burden.
7. Bumped gids.html stylesheet cache version from v=16 to v=19 so the browser does not keep serving the old layout CSS.

Files changed:
- css/ads-only-accor-v6.css
- js/fids-core.js
- gids.html

Scope:
- Ads only. Flight logic, gate logic, weather, maps, aircraft cards, carousel timing, and admin controls were not intentionally changed.
