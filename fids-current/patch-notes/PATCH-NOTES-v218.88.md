# v218.88 — Gate recovery pass

- Reduced oversized gate header and timing banner typography so fields do not collide.
- Restored the center ad console as a two-part rail: logo panel plus rotating ad/still carousel.
- Removed the broken top-left floating ad-logo box.
- Normalized ICAO operator prefixes like POE to IATA codes like PD before deciding whether to show `Operated by`.
- Porter flights no longer show `Operated by porter` when Porter is the marketing carrier.
- Kept true partner operators visible, for example Air Canada operated by Jazz.
- Kept real aircraft PNGs only, pinned to the bottom edge, with no fake aircraft fallback.
- Lightened and tightened the aircraft card so it does not swallow the display.
