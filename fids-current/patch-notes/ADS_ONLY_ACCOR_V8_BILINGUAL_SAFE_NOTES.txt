ADS-ONLY ACCOR V8 BILINGUAL SAFE

Built from V6, not V7, to undo the forced 9-language live rotation.

What this fixes:
- Live signage default returns to Canada's primary languages: English/French.
- All 9 languages remain available in the menu:
  en, fr, es, de, it, pt, ja, zh, ar.
- Removed the old max-3 language selection cap.
- Accor ad labels now localize with the active language.
- French Accor slides no longer show English-only hotel description text when localized hotel copy is missing; they use a neutral French fallback.
- Star/review labels localize.
- Amenities area is forced visible and made more compact.

Scope:
- Ads only.
- Preserves menu, flight logic, gate logic, weather, maps, aircraft cards, carousel timing, and admin controls.
- No performance experiments.
- No operational layout rewrite.

Known:
- If some operational labels are hardcoded in the original gate template, they need a separate label-only localization pass.
- Proper hotel names and street addresses stay as proper nouns.
