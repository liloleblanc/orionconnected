# v218.90 Gate rescue pass

- Restored the gate center panel to true ad/still rotation only.
- Removed the broken amenities/local-info first slide that created the empty dark slab.
- Fixed inbound origin duplication such as `Toronto (YYZ) (YYZ)`.
- Kept “This flight is coming from” and “Time left for arrival”.
- Kept aircraft type under the aircraft and forced the label spacing to render correctly.
- Re-tightened the left aircraft stack so the aircraft stays anchored at the bottom without sprawling.
